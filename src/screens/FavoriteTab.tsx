import React from 'react';
import { View, Text } from 'react-native';

const FavoriteTabScreen = () => (
  <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
    <Text>Seconda Tab</Text>
  </View>
);

export default FavoriteTabScreen;
